请你对当前 skill 做一次完整的设计与实现一致性审计。不要自动修复；先把发现的问题和建议修复内容返回给用户，等待用户确认要修复哪些内容后，再进行修复。

审计重点：
1. 职责边界：这个 skill 是否只做它自己声明的事情？有没有引用、依赖、暗示其他 skill 或外部流程？
2. 命名一致性：文件名、配置名、账号名、命令示例、文档标题是否统一？有没有旧名称残留？
3. 流程边界：主 SKILL.md 负责哪些步骤，references 只负责哪些补充？有没有重复描述、职责倒置、误导性的“集成”说明？
4. 配置设计：所有可配置项是否集中保存在 `config.yaml` 中，`config.yaml.example` 是否最小、清晰、安全，并能完整反映运行所需配置？skill 里的真实配置与示例配置命名是否统一，真实配置是否被 `.gitignore` 忽略？是否存在散落在脚本常量、环境变量说明、README、references 或命令示例里的隐式配置，未同步到 `config.yaml.example`？
5. 配置引导与校验：如果skill需要用户配置的话，skill流程的第一步是否先提示用户输入或确认这些配置；如果不同配置会导致不同运行路径，是否清楚说明各自可用功能和限制？（是否提示了agent需要等待用户的输入，而不是伪造配置）
6. 路径可移植性：不要使用绝对路径。如果 skill 要访问本 skill 的其他目录，是否使用相对路径或基于 skill 根目录解析？如果要访问 skill 文件夹之外的其他文件夹（这些路径在不同用户电脑上可能不一样），是否通过配置文件config.yaml配置，而不是写死在文档、脚本或示例中？
7. 脚本垃圾内容：脚本里是否保留了本 skill 不需要的旧参数、旧命令、旧模块、死代码, 是否有冗余或者过时的文件？
8. 用户视角：一个新 agent 只读这个 skill，会不会知道该怎么执行？会不会被旧文档或旧示例带偏？
9. 验证计划：如果后续执行修复，应该运行哪些必要的语法检查、配置解析检查、help 输出检查？

要求：
- 密钥信息或者是一些账号信息可以存在 config.yaml 文档里，但是必须被 git ignore 忽略
- 先快速阅读文件结构和 SKILL.md，不要只修我指出的点。
- 不要自动修改、删除、移动任何文件。
- 对每个问题给出建议修复内容、影响范围和优先级。
- 等用户明确确认修复范围后，才执行具体修改。
- 保持 scope 小，不引入新功能。
- 最后给出：发现的问题、建议修复内容、建议验证方式、需要人工确认的事项。
